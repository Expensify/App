pre-commit:
  commands:
    oxfmt:
      glob:
        - '*.{j,mj,cj}s'
        - '*.{ts,tsx,mts,cts}'
        - '*.{yml,yaml}'
      run: npx oxfmt --write --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
