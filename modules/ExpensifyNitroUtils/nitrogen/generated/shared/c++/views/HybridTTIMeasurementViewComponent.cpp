///
/// HybridTTIMeasurementViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridTTIMeasurementViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::utils::views {

  extern const char HybridTTIMeasurementViewComponentName[] = "TTIMeasurementView";

  HybridTTIMeasurementViewProps::HybridTTIMeasurementViewProps(const react::PropsParserContext& context,
                                                               const HybridTTIMeasurementViewProps& sourceProps,
                                                               const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    onMeasurement([&]() -> CachedProp<std::function<void(const TTIMeasurementValue& /* measurement */)>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onMeasurement", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onMeasurement;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::function<void(const TTIMeasurementValue& /* measurement */)>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.onMeasurement);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("TTIMeasurementView.onMeasurement: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridTTIMeasurementViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridTTIMeasurementViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("TTIMeasurementView.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridTTIMeasurementViewProps::HybridTTIMeasurementViewProps(const HybridTTIMeasurementViewProps& other):
    react::ViewProps(),
    onMeasurement(other.onMeasurement),
    hybridRef(other.hybridRef) { }

  bool HybridTTIMeasurementViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("onMeasurement"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridTTIMeasurementViewComponentDescriptor::HybridTTIMeasurementViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridTTIMeasurementViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                              const std::shared_ptr<const react::Props>& props,
                                                                                              react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridTTIMeasurementViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridTTIMeasurementViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridTTIMeasurementViewShadowNode&>(shadowNode);
    const HybridTTIMeasurementViewProps& props = concreteShadowNode.getConcreteProps();
    HybridTTIMeasurementViewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::utils::views
