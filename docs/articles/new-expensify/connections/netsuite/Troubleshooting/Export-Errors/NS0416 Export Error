---
title: NS0416 Export Error: Error Creating Vendor – Email Field Not Visible
description: Learn why the NS0416 export error occurs and how to make the Email field visible on the preferred NetSuite export form.
keywords: NS0416, NetSuite error creating vendor, email field not visible NetSuite, vendor creation error NetSuite, transaction form email field, Expensify NetSuite integration
internalScope: Audience is Workspace Admins using the NetSuite integration. Covers the NS0416 export error related to Email field visibility on NetSuite transaction forms. Does not cover vendor subsidiary or email mismatch issues.
---

# NS0416 Export Error: Error Creating Vendor – Email Field Not Visible

If you see the error message **“NS0416 Export Error: Error creating vendor. Please ensure that the 'Email' field is set to 'Show' on the preferred export type form in NetSuite.”**, it means the **Email** field is hidden on the NetSuite form used for exports.

Expensify must be able to populate the Email field when creating or updating vendor records.

---

## Why the NS0416 Export Error Happens

The NS0416 export error occurs when the required **Email** field is not visible on the preferred NetSuite transaction form.

If the field is hidden, NetSuite blocks Expensify from creating the vendor record.

---

## How to Fix the NS0416 Export Error

### Step 1: Make the Email Field Visible in NetSuite

In NetSuite:

1. Go to **Customization > Forms > Transaction Forms**.
2. Click **Edit** next to the export form marked as preferred.
3. Locate the **Email** field.
4. Ensure the field is marked as **Show**.
5. Save your changes.

---

### Step 2: Sync the Connection in Expensify

After updating the form:

On web:

1. Go to the **navigation tabs on the left** and select **Workspaces**.
2. Select your Workspace.
3. Select **Accounting**.
4. Click the **three-dot icon** next to the NetSuite connection.
5. Click **Sync now**.

On mobile:

1. Tap the **navigation tabs on the bottom** and select **Workspaces**.
2. Select your Workspace.
3. Tap **Accounting**.
4. Tap the **three-dot icon** next to the NetSuite connection.
5. Tap **Sync now**.

Retry exporting the report once the sync completes.

---

# FAQ

## Does the NS0416 Export Error only affect new vendors?

Yes. This error typically appears when Expensify attempts to create a new vendor record.

## Do I need NetSuite admin access to fix the NS0416 Export Error?

Yes. Updating transaction form field visibility requires appropriate NetSuite permissions.
