---
title: NS0077 Export Error: Could Not Create Employee
description: Learn why the NS0077 export error occurs and how to sync your Workspace or contact Concierge to resolve employee creation permission issues in NetSuite.
keywords: NS0077, NetSuite could not create employee, employee record permission error NetSuite, Expensify NetSuite export error, sync workspace NetSuite
internalScope: Audience is Workspace Admins using the NetSuite integration. Covers the NS0077 export error related to employee record creation and Workspace sync status. Does not cover detailed role permission configuration steps.
---

# NS0077 Export Error: Could Not Create Employee

If you see the error message **“NS0077 Export Error: Could not create 'employee.' Your current role does not have permission to access this record in NetSuite.”**, it means Expensify attempted to create an employee record in NetSuite but was blocked by permissions.

This typically happens when the integration role does not have sufficient access or the Workspace has not recently synced.

---

## Why the NS0077 Export Error Happens

The NS0077 export error occurs when:

- An employee record needs to be created in NetSuite during export, and  
- The connected role does not have permission to create or access Employee records  

It can also occur if the Workspace has not synced recently and employee data is outdated.

---

## How to Fix the NS0077 Export Error

### Step 1: Confirm the Workspace Has Synced Recently

In Expensify:

On web:

1. Go to the **navigation tabs on the left** and select **Workspaces**.
2. Select your Workspace.
3. Select **Accounting**.
4. Confirm the Workspace has synced within the last 24 hours.

If it has not synced recently:

1. Click the **three-dot icon** next to the NetSuite connection.
2. Click **Sync now**.
3. Retry exporting the report.

On mobile:

1. Tap the **navigation tabs on the bottom** and select **Workspaces**.
2. Select your Workspace.
3. Tap **Accounting**.
4. Tap the **three-dot icon** next to the NetSuite connection.
5. Tap **Sync now**.
6. Retry exporting the report.

---

### Step 2: Contact Concierge if the Error Persists

If the Workspace has synced within the last 24 hours and the error continues, reach out to **Concierge** for further assistance.

Additional role permissions or configuration updates in NetSuite may be required.

---

# FAQ

## Does the NS0077 Export Error affect all new employees?

It may affect exports for employees who do not already exist in NetSuite.

## Do I need NetSuite admin access to fix the NS0077 Export Error?

You may need NetSuite admin assistance if additional role permissions are required.
