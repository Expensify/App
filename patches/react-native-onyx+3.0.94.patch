diff --git a/node_modules/react-native-onyx/dist/OnyxCache.js b/node_modules/react-native-onyx/dist/OnyxCache.js
index c3bede4..6151bc6 100644
--- a/node_modules/react-native-onyx/dist/OnyxCache.js
+++ b/node_modules/react-native-onyx/dist/OnyxCache.js
@@ -8,6 +8,7 @@ const fast_equals_1 = require("fast-equals");
 const bindAll_1 = __importDefault(require("lodash/bindAll"));
 const utils_1 = __importDefault(require("./utils"));
 const OnyxKeys_1 = __importDefault(require("./OnyxKeys"));
+const __probe = require("./__deepEqualProbe");
 /**
  * Stable frozen empty object used as the canonical value for empty collections.
  * Returning the same reference avoids unnecessary re-renders in useSyncExternalStore,
@@ -208,7 +209,7 @@ class OnyxCache {
         if (currentValue === value) {
             return false;
         }
-        return !(0, fast_equals_1.deepEqual)(currentValue, value);
+        return !__probe.timedDeepEqual("cache.hasValueChanged", key, "", currentValue, value);
     }
     /**
      * Sets the list of keys that are considered safe for eviction
diff --git a/node_modules/react-native-onyx/dist/__deepEqualProbe.js b/node_modules/react-native-onyx/dist/__deepEqualProbe.js
new file mode 100644
index 0000000..0659749
--- /dev/null
+++ b/node_modules/react-native-onyx/dist/__deepEqualProbe.js
@@ -0,0 +1,73 @@
+'use strict';
+// TEMP PROFILING PROBE - not part of the package. Delete (or re-run npm i) when done.
+Object.defineProperty(exports, '__esModule', {value: true});
+const {deepEqual} = require('fast-equals');
+
+const stats = new Map();
+
+function record(source, key, site, ms, wasEqual) {
+    const id = `${source}|${key}|${site}`;
+    let entry = stats.get(id);
+    if (!entry) {
+        entry = {source, key, site, calls: 0, ms: 0, maxMs: 0, equal: 0};
+        stats.set(id, entry);
+    }
+    entry.calls++;
+    entry.ms += ms;
+    entry.equal += wasEqual ? 1 : 0;
+    if (ms > entry.maxMs) {
+        entry.maxMs = ms;
+    }
+    if (ms >= probe.warnMs) {
+        console.warn(`[deepEqualProbe] ${ms.toFixed(1)}ms ${source} key=${key} ${site}`);
+    }
+}
+
+function timedDeepEqual(source, key, site, a, b) {
+    const t0 = performance.now();
+    const isEqual = deepEqual(a, b);
+    record(source, key, site, performance.now() - t0, isEqual);
+    return isEqual;
+}
+
+// Captures the creation site of a selector so a hot entry points at a component file.
+const NOISE = /__deepEqualProbe|react-dom|react\/cjs|react-refresh|scheduler|node_modules[\\/]react[\\/]|useOnyx|Error$/;
+function callSite() {
+    const lines = (new Error().stack || '').split('\n');
+    const frames = lines.map((l) => l.trim().replace(/^at\s+/, '')).filter((l) => l && !NOISE.test(l));
+    return frames.slice(0, 3).join(' < ') || lines.slice(1, 6).join(' | ');
+}
+
+const probe = {
+    warnMs: 2,
+    stats,
+    reset: () => stats.clear(),
+    dump: (limit = 20) => {
+        const rows = [...stats.values()]
+            .sort((a, b) => b.ms - a.ms)
+            .slice(0, limit)
+            .map((e) => ({
+                source: e.source,
+                key: e.key,
+                calls: e.calls,
+                ms: +e.ms.toFixed(1),
+                avgMs: +(e.ms / e.calls).toFixed(2),
+                maxMs: +e.maxMs.toFixed(1),
+                wastedPct: Math.round((e.equal / e.calls) * 100),
+                site: e.site,
+            }));
+        // console.warn, not log/table: production babel strips everything except error/warn.
+        console.warn(
+            `[deepEqualProbe] total ${[...stats.values()].reduce((s, e) => s + e.ms, 0).toFixed(1)}ms in ${[...stats.values()].reduce((s, e) => s + e.calls, 0)} calls\n${rows
+                .map((r) => `${r.ms}ms ${r.calls}x avg${r.avgMs} max${r.maxMs} waste${r.wastedPct}% ${r.source} ${r.key}\n    ${r.site}`)
+                .join('\n')}`,
+        );
+        return rows;
+    },
+};
+
+globalThis.__onyxDeepEqual = probe;
+
+exports.timedDeepEqual = timedDeepEqual;
+exports.callSite = callSite;
+exports.default = probe;
diff --git a/node_modules/react-native-onyx/dist/useOnyx.js b/node_modules/react-native-onyx/dist/useOnyx.js
index 63ec225..64d8998 100644
--- a/node_modules/react-native-onyx/dist/useOnyx.js
+++ b/node_modules/react-native-onyx/dist/useOnyx.js
@@ -44,6 +44,7 @@ const OnyxUtils_1 = __importDefault(require("./OnyxUtils"));
 const OnyxSnapshotCache_1 = __importDefault(require("./OnyxSnapshotCache"));
 const memoizedShallowEqual_1 = __importDefault(require("./memoizedShallowEqual"));
 const useLiveRef_1 = __importDefault(require("./useLiveRef"));
+const __probe = require("./__deepEqualProbe");
 function useOnyx(key, options, dependencies = []) {
     const connectionRef = (0, react_1.useRef)(null);
     const currentDependenciesRef = (0, useLiveRef_1.default)(dependencies);
@@ -57,6 +58,7 @@ function useOnyx(key, options, dependencies = []) {
         let lastOutput;
         let lastDependencies = [];
         let hasComputed = false;
+        const __probeSite = __probe.callSite();
         return (input) => {
             const currentDependencies = currentDependenciesRef.current;
             // Recompute if input changed, dependencies changed, or first time
@@ -67,7 +69,7 @@ function useOnyx(key, options, dependencies = []) {
                 // when the same input is seen again (even if the output didn't change).
                 lastInput = input;
                 // Only update the output reference if it actually changed
-                if (!hasComputed || !(0, fast_equals_1.deepEqual)(lastOutput, newOutput) || dependenciesChanged) {
+                if (!hasComputed || !__probe.timedDeepEqual("useOnyx", key, __probeSite, lastOutput, newOutput) || dependenciesChanged) {
                     lastOutput = newOutput;
                     lastDependencies = [...currentDependencies];
                     hasComputed = true;
