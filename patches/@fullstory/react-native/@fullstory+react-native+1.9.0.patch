diff --git a/node_modules/@fullstory/react-native/ios/FullStory.mm b/node_modules/@fullstory/react-native/ios/FullStory.mm
index bd30520..26a834a 100644
--- a/node_modules/@fullstory/react-native/ios/FullStory.mm
+++ b/node_modules/@fullstory/react-native/ios/FullStory.mm
@@ -22,6 +22,11 @@
     return self;
 }
 
+- (void)invalidate {
+    // Clear the event emitter callback to prevent a use-after-free crash.
+    _eventEmitterCallback = nullptr;
+}
+
 NSString *const PagesAPIError = @"Unable to access native FullStory pages API and call %@. Pages API will not function correctly. Make sure that your plugin is at least version 1.41; if the issue persists, please contact FullStory Support.";
 
 RCT_EXPORT_MODULE()
@@ -200,11 +205,13 @@ RCT_EXPORT_METHOD(updatePage:(NSString *)nonce pageProperties:(NSDictionary *)pa
     }
 
 #ifdef RCT_NEW_ARCH_ENABLED
-    [self emitOnSessionStarted:@{
-        @"replayStartUrl": sessionUrl,
-        @"replayNowUrl": [FS currentSessionURL: true] ?: @"",
-        @"sessionId": FS.currentSession ?: @"",
-    }];
+    if (_eventEmitterCallback) {
+        [self emitOnSessionStarted:@{
+            @"replayStartUrl": sessionUrl,
+            @"replayNowUrl": [FS currentSessionURL: true] ?: @"",
+            @"sessionId": FS.currentSession ?: @"",
+        }];
+    }
 #endif
 }
 
