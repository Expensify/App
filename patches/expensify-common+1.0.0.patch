diff --git a/node_modules/expensify-common/lib/str.js b/node_modules/expensify-common/lib/str.js
index 0aa2aac..28c4f7a 100644
--- a/node_modules/expensify-common/lib/str.js
+++ b/node_modules/expensify-common/lib/str.js
@@ -4,6 +4,8 @@ import {AllHtmlEntities} from 'html-entities';
 import replaceAll from 'string.prototype.replaceall';
 import {CONST} from './CONST';
 
+const REMOVE_SMS_DOMAIN_PATTERN = new RegExp(`@${CONST.SMS.DOMAIN}`, 'gi');
+
 const Str = {
 
     /**
@@ -929,8 +931,7 @@ const Str = {
      * @return {String}
      */
     removeSMSDomain(text) {
-        const removePattern = new RegExp(`@${CONST.SMS.DOMAIN}`, 'gi');
-        return text.replace(removePattern, '');
+        return text.replace(REMOVE_SMS_DOMAIN_PATTERN, '');
     },
 
     /**
