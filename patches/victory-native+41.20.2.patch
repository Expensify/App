diff --git a/node_modules/victory-native/dist/pie/PieSliceAngularInset.js b/node_modules/victory-native/dist/pie/PieSliceAngularInset.js
index 7e5356f..1d8b81b 100644
--- a/node_modules/victory-native/dist/pie/PieSliceAngularInset.js
+++ b/node_modules/victory-native/dist/pie/PieSliceAngularInset.js
@@ -23,7 +23,7 @@ const AnimatedPath_1 = require("../cartesian/components/AnimatedPath");
 const PieSliceAngularInset = (props) => {
     const { angularInset, children, animate } = props, rest = __rest(props, ["angularInset", "children", "animate"]);
     const { slice } = (0, PieSliceContext_1.usePieSliceContext)();
-    const [path, insetPaint] = (0, useSliceAngularInsetPath_1.useSliceAngularInsetPath)({ slice, angularInset });
+    const [path] = (0, useSliceAngularInsetPath_1.useSliceAngularInsetPath)({ slice, angularInset });
     // If the path is empty, don't render anything
     if (path.toSVGString() === "M0 0L0 0M0 0L0 0") {
         return null;
@@ -32,7 +32,12 @@ const PieSliceAngularInset = (props) => {
         return null;
     }
     const Component = animate ? AnimatedPath_1.AnimatedPath : react_native_skia_1.Path;
-    return (<Component path={path} paint={insetPaint} animate={animate} {...rest}>
+    // Pass color/style/strokeWidth as individual props instead of via the `paint` prop.
+    // Using `paint` triggers a short-circuit in react-native-skia's Player.js that (a) drops
+    // any other PathProps like `blendMode`, and (b) pushes only to the paints stack without
+    // pushing to the opacities stack, which then unbalances restorePaint() and causes subsequent
+    // draws to render with NaN alpha (invisible).
+    return (<Component path={path} color={angularInset.angularStrokeColor} style="stroke" strokeWidth={angularInset.angularStrokeWidth} animate={animate} {...rest}>
       {children}
     </Component>);
 };
diff --git a/node_modules/victory-native/src/pie/PieSliceAngularInset.tsx b/node_modules/victory-native/src/pie/PieSliceAngularInset.tsx
index bede090..ec99fe6 100644
--- a/node_modules/victory-native/src/pie/PieSliceAngularInset.tsx
+++ b/node_modules/victory-native/src/pie/PieSliceAngularInset.tsx
@@ -21,7 +21,7 @@ type PieSliceAngularInsetProps = {
 export const PieSliceAngularInset = (props: PieSliceAngularInsetProps) => {
   const { angularInset, children, animate, ...rest } = props;
   const { slice } = usePieSliceContext();
-  const [path, insetPaint] = useSliceAngularInsetPath({ slice, angularInset });
+  const [path] = useSliceAngularInsetPath({ slice, angularInset });
 
   // If the path is empty, don't render anything
   if (path.toSVGString() === "M0 0L0 0M0 0L0 0") {
@@ -33,8 +33,20 @@ export const PieSliceAngularInset = (props: PieSliceAngularInsetProps) => {
   }
 
   const Component = animate ? AnimatedPath : Path;
+  // Pass color/style/strokeWidth as individual props instead of via the `paint` prop.
+  // Using `paint` triggers a short-circuit in react-native-skia's Player.js that (a) drops
+  // any other PathProps like `blendMode`, and (b) pushes only to the paints stack without
+  // pushing to the opacities stack, which then unbalances restorePaint() and causes subsequent
+  // draws to render with NaN alpha (invisible). See bug investigation in PieChartContent.tsx.
   return (
-    <Component path={path} paint={insetPaint} animate={animate} {...rest}>
+    <Component
+      path={path}
+      color={angularInset.angularStrokeColor}
+      style="stroke"
+      strokeWidth={angularInset.angularStrokeWidth}
+      animate={animate}
+      {...rest}
+    >
       {children}
     </Component>
   );
