diff --git a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/views/textinput/ReactEditText.kt b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/views/textinput/ReactEditText.kt
index 477893c..282a374 100644
--- a/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/views/textinput/ReactEditText.kt
+++ b/node_modules/react-native/ReactAndroid/src/main/java/com/facebook/react/views/textinput/ReactEditText.kt
@@ -143,6 +143,7 @@ public open class ReactEditText public constructor(context: Context) : AppCompat
   private var autoFocus = false
   private var contextMenuHidden = false
   private var didAttachToWindow = false
+  private var hasSelectedTextOnFocus = false
   private var selectTextOnFocus = false
   private var placeholder: String? = null
   private var overflow = Overflow.VISIBLE
@@ -295,11 +296,11 @@ public open class ReactEditText public constructor(context: Context) : AppCompat
 
   override fun onLayout(changed: Boolean, left: Int, top: Int, right: Int, bottom: Int) {
     onContentSizeChange()
-    if (selectTextOnFocus && isFocused) {
+    if (selectTextOnFocus && isFocused && !hasSelectedTextOnFocus) {
       // Explicitly call this method to select text when layout is drawn
       selectAll()
       // Prevent text on being selected for next layout pass
-      selectTextOnFocus = false
+      hasSelectedTextOnFocus = false
     }
   }
 
@@ -495,6 +496,7 @@ public open class ReactEditText public constructor(context: Context) : AppCompat
       showSoftKeyboard()
     }
 
+    hasSelectedTextOnFocus = true
     return focused
   }
 
