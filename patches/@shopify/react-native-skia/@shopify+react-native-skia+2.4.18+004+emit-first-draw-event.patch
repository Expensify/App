diff --git a/node_modules/@shopify/react-native-skia/lib/commonjs/views/SkiaPictureView.web.js b/node_modules/@shopify/react-native-skia/lib/commonjs/views/SkiaPictureView.web.js
index cad87bc..82503a2 100644
--- a/node_modules/@shopify/react-native-skia/lib/commonjs/views/SkiaPictureView.web.js
+++ b/node_modules/@shopify/react-native-skia/lib/commonjs/views/SkiaPictureView.web.js
@@ -24,6 +24,15 @@ const dp2Pixel = (pd, rect) => {
     height: rect.height * pd
   };
 };
+const notifyFirstDraw = renderer => {
+  if (renderer.hasDrawn) {
+    return;
+  }
+  renderer.hasDrawn = true;
+  renderer.canvas.dispatchEvent(new CustomEvent("skia-first-draw", {
+    bubbles: true
+  }));
+};
 class WebGLRenderer {
   constructor(canvas, pd) {
     this.canvas = canvas;
@@ -95,6 +104,7 @@ class WebGLRenderer {
       canvas.drawPicture(picture);
       canvas.restore();
       this.surface.ref.flush();
+      notifyFirstDraw(this);
     }
   }
   dispose() {
@@ -176,6 +186,7 @@ class StaticWebGLRenderer {
     // Draw the tempCanvas scaled down to the display size
     ctx2d.drawImage(tempCanvas, 0, 0, tempCanvas.width, tempCanvas.height, 0, 0, this.canvas.clientWidth * this.pd, this.canvas.clientHeight * this.pd);
     this.cleanupWebGLContext(tempCanvas);
+    notifyFirstDraw(this);
   }
   makeImageSnapshot(picture, rect) {
     if (!this.cachedImage) {
diff --git a/node_modules/@shopify/react-native-skia/lib/module/views/SkiaPictureView.web.js b/node_modules/@shopify/react-native-skia/lib/module/views/SkiaPictureView.web.js
index 12ea0ae..3ee87cd 100644
--- a/node_modules/@shopify/react-native-skia/lib/module/views/SkiaPictureView.web.js
+++ b/node_modules/@shopify/react-native-skia/lib/module/views/SkiaPictureView.web.js
@@ -18,6 +18,15 @@ const dp2Pixel = (pd, rect) => {
     height: rect.height * pd
   };
 };
+const notifyFirstDraw = renderer => {
+  if (renderer.hasDrawn) {
+    return;
+  }
+  renderer.hasDrawn = true;
+  renderer.canvas.dispatchEvent(new CustomEvent("skia-first-draw", {
+    bubbles: true
+  }));
+};
 class WebGLRenderer {
   constructor(canvas, pd) {
     this.canvas = canvas;
@@ -89,6 +98,7 @@ class WebGLRenderer {
       canvas.drawPicture(picture);
       canvas.restore();
       this.surface.ref.flush();
+      notifyFirstDraw(this);
     }
   }
   dispose() {
@@ -170,6 +180,7 @@ class StaticWebGLRenderer {
     // Draw the tempCanvas scaled down to the display size
     ctx2d.drawImage(tempCanvas, 0, 0, tempCanvas.width, tempCanvas.height, 0, 0, this.canvas.clientWidth * this.pd, this.canvas.clientHeight * this.pd);
     this.cleanupWebGLContext(tempCanvas);
+    notifyFirstDraw(this);
   }
   makeImageSnapshot(picture, rect) {
     if (!this.cachedImage) {
