diff --git a/node_modules/react-native-screens/src/components/helpers/DelayedFreeze.tsx b/node_modules/react-native-screens/src/components/helpers/DelayedFreeze.tsx
index 443da63..a1b2c3d 100644
--- a/node_modules/react-native-screens/src/components/helpers/DelayedFreeze.tsx
+++ b/node_modules/react-native-screens/src/components/helpers/DelayedFreeze.tsx
@@ -12,11 +12,16 @@ function DelayedFreeze({ freeze, children }: FreezeWrapperProps) {
   // flag used for determining whether freeze should be enabled
   const [freezeState, setFreezeState] = React.useState(false);

+  // We use startTransition to mark the freeze as a low-priority update so it
+  // doesn't starve pending macrotasks (setTimeout, rAF). Without this, the
+  // Suspense boundary used by react-freeze can block queued callbacks from firing.
   React.useEffect(() => {
     const id = setTimeout(() => {
-      setFreezeState(freeze);
+      React.startTransition(() => {
+        setFreezeState(freeze);
+      });
     }, 0);
     return () => {
       clearTimeout(id);