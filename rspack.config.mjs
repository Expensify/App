// Native config lives in config/repack/rspack.config.mjs. Re.Pack only discovers the config from the
// project root, so this re-export keeps it working.
export {default} from './config/repack/rspack.config.mjs';
