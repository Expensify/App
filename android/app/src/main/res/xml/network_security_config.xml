<?xml version="1.0" encoding="utf-8"?>
<!--
    Certificate pinning configuration (Iteration 1 - NewDot, release builds).

    This <pin-set> enforces SSL public-key pinning for Expensify-controlled hosts across all
    platform TLS stacks that honor network security config: HttpURLConnection, WebView, and Glide.
    OkHttp traffic (fetch(), react-native-blob-util, etc.) is pinned separately in CertificatePinning.kt.

    Keep the hashes here in sync with config/certificatePinning/pins.json and CertificatePinning.kt /
    CertificatePinning.swift (regenerate via scripts/generateCertificatePins.sh).

    Each domain pins the leaf SPKI hash (primary) plus its issuing intermediate CA SPKI hash
    (durable backup that survives leaf rotation). Do NOT add an `expiration` attribute - an expired
    pin-set silently disables pinning; rotation is managed through app updates instead.

    Debug builds use res/xml/network_security_config_debug.xml (no pinning) so local dev servers and
    proxy tooling (Charles/Proxyman) keep working.
-->
<network-security-config>
    <!-- Group A: leaf CN=expensify.com + Let's Encrypt YE1 intermediate -->
    <domain-config>
        <domain includeSubdomains="false">www.expensify.com</domain>
        <domain includeSubdomains="false">secure.expensify.com</domain>
        <domain includeSubdomains="false">staging.expensify.com</domain>
        <domain includeSubdomains="false">staging-secure.expensify.com</domain>
        <pin-set>
            <pin digest="SHA-256">cSP5K9Slk59AgwZPst+dLPuNE+ZhypUlYRQNW1XC/fc=</pin>
            <pin digest="SHA-256">brzvtCELCIZUo4sD/qPX0ccRtPsd3DY6RfmxpOU9oB4=</pin>
        </pin-set>
    </domain-config>

    <!-- Group B: leaf CN=expensify.com + Google Trust Services WE1 intermediate -->
    <domain-config>
        <domain includeSubdomains="false">new.expensify.com</domain>
        <domain includeSubdomains="false">staging.new.expensify.com</domain>
        <pin-set>
            <pin digest="SHA-256">G2v6PWWl92F5vVHCtAYwScBHqNtPMkxb++SFoBJq5F4=</pin>
            <pin digest="SHA-256">kIdp6NNEd8wsugYyyIYFsi1ylMCED3hZbSR8ZFsa/A4=</pin>
        </pin-set>
    </domain-config>

    <!-- Group C: integrations leaf + Let's Encrypt R13 intermediate -->
    <domain-config>
        <domain includeSubdomains="false">integrations.expensify.com</domain>
        <pin-set>
            <pin digest="SHA-256">7D0dEgdEKEMYRTgVwvnhJv19B4apk0QM/GPnRAKRGUs=</pin>
            <pin digest="SHA-256">AlSQhgtJirc8ahLyekmtX+Iw+v46yPYRLJt9Cq1GlB0=</pin>
        </pin-set>
    </domain-config>

    <!-- Group D: travel leaf + Google Trust Services WE1 intermediate -->
    <domain-config>
        <domain includeSubdomains="false">travel.expensify.com</domain>
        <domain includeSubdomains="false">staging.travel.expensify.com</domain>
        <pin-set>
            <pin digest="SHA-256">Qb3qmTdRt/xHEN5PVtn+YhKoGqF/lhRX88cSFuSCJqM=</pin>
            <pin digest="SHA-256">kIdp6NNEd8wsugYyyIYFsi1ylMCED3hZbSR8ZFsa/A4=</pin>
        </pin-set>
    </domain-config>

    <!-- Group E: CloudFront CDN leaf + Amazon RSA 2048 M01 intermediate -->
    <domain-config>
        <domain includeSubdomains="false">d2k5nsl2zxldvw.cloudfront.net</domain>
        <pin-set>
            <pin digest="SHA-256">P9HBoLji8YncXSnb0AnAm72fJO/vpmxZrsl4fvUBkxc=</pin>
            <pin digest="SHA-256">DxH4tt40L+eduF6szpY6TONlxhZhBd+pJ9wbHlQ2fuw=</pin>
        </pin-set>
    </domain-config>
</network-security-config>
