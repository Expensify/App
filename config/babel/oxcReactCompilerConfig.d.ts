/** CJS implementation in `oxcReactCompilerConfig.js`, typed so the Rsbuild TS configs can import it. */
declare function oxcReactCompilerConfig(overrides?: Record<string, unknown>): Record<string, unknown>;

export = oxcReactCompilerConfig;
