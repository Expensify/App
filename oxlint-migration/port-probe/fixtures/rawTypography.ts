// rulesdir/no-raw-typography: a raw pixel fontSize cannot track the typography scale, so it drifts
// from the design system the moment the scale changes.
const styles = {
    label: {
        fontSize: 13,
    },
};

export default styles;
