import type RegisterPrefetchTokenRefresh from './types';

const NOOP = () => {};

const registerPrefetchTokenRefresh: RegisterPrefetchTokenRefresh = NOOP;

export default registerPrefetchTokenRefresh;
