type OpenSearchPageParams = {
    /**
     * Whether to include partially setup bank accounts in the response.
     */
    includePartiallySetupBankAccounts?: boolean;
};

export default OpenSearchPageParams;

