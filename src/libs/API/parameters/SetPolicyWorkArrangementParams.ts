type SetPolicyWorkArrangementParams = {
    policyID: string;
    /** True when members commute to an office, false when they have no regular workplace */
    isOffice: boolean;
};

export default SetPolicyWorkArrangementParams;
