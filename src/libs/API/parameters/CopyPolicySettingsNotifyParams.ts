type CopyPolicySettingsNotifyParams = {
    /** When true, only notify via Concierge if the copy fails (set when the user dismisses the in-progress modal) */
    shouldOnlyNotifyOnFailure: boolean;
};

export default CopyPolicySettingsNotifyParams;
