type UpdateDualEntryExporterParams = {
    /** Workspace whose preferred exporter is updated. */
    policyID: string;

    /** Email address of the preferred exporter. */
    email: string;
};

export default UpdateDualEntryExporterParams;
