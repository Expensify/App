// Web RHP geometry follows the JS stack's animated values.
function getRHPLayoutValue<T>(_value: number, animatedValue: T): T | number {
    return animatedValue;
}

export default getRHPLayoutValue;
