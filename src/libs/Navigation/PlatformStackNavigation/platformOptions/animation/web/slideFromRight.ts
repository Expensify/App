import type {StackNavigationOptions} from '@react-navigation/stack';

const slideFromRight: StackNavigationOptions = {animationEnabled: true, animationTypeForReplace: 'push', gestureDirection: 'horizontal'};

export default slideFromRight;
