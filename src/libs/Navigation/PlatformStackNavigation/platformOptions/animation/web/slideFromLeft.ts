import type {StackNavigationOptions} from '@react-navigation/stack';

const slideFromLeft: StackNavigationOptions = {animationEnabled: true, gestureDirection: 'horizontal-inverted'};

export default slideFromLeft;
