import type TrackConciergeResponse from './types';

// The browser favicon has no native equivalent. Keep native notification badges based on unread counts.
const trackConciergeResponse: TrackConciergeResponse = () => {};

export default trackConciergeResponse;
