import type NavBarManager from './types';

const navBarManager: NavBarManager = {
    setTheme: () => {},
    setButtonStyle: () => {},
    setBackgroundColor: () => {},
};

export default navBarManager;
