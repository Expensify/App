/**
 * Returns the URI string. For non-ios devices, we don't need to change anything
 *  @param {bool} localFileUri which is fileUri coming in by default - from react native picker
 * @return {String}
 */

export default localFileUri => localFileUri;
