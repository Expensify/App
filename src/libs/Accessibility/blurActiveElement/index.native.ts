/** No-op on native; the parameter exists only to match the web signature. */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const blurActiveElement = (_container?: unknown) => {};

export default blurActiveElement;
