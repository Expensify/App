import React from 'react';
import {View} from 'react-native';
import Text from '../Text';

const propTypes = {};

const defaultProps = {};

const SelectionListMultiple = () => {
    return (
        <View>
            <Text>SelectionListMultiple</Text>
        </View>
    );
};

SelectionListMultiple.displayName = 'SelectionListMultiple';
SelectionListMultiple.defaultProps = defaultProps;
SelectionListMultiple.propTypes = propTypes;

export default SelectionListMultiple;
