import React from 'react';
import {View} from 'react-native';
import Text from '../Text';

const propTypes = {};

const defaultProps = {};

const SelectionListItemMultiple = () => {
    return (
        <View>
            <Text>SelectionListItemMultiple</Text>
        </View>
    );
};

SelectionListItemMultiple.displayName = 'SelectionListItemMultiple';
SelectionListItemMultiple.defaultProps = defaultProps;
SelectionListItemMultiple.propTypes = propTypes;

export default SelectionListItemMultiple;
