import type {Role} from 'react-native';

type GetContainerRole = (canSelectMultiple: boolean) => Role | undefined;

// eslint-disable-next-line import/prefer-default-export
export type {GetContainerRole};
