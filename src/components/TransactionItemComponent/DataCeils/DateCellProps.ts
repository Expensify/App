import type Transaction from '@src/types/onyx/Transaction';

type DataCellProps = {
    transactionItem:Transaction,
    showTooltip:boolean,
    isLargeScreenWidth?:boolean,
}

export default DataCellProps
