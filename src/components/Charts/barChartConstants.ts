/** Inner padding between bars (0.3 = 30% of bar width) */
const BAR_INNER_PADDING = 0.3;

/** Corner radius (px) applied to the ends of a bar */
const BAR_CORNER_RADIUS = 8;

/** Minimum vertical space (px) reserved for one category row in a horizontal bar chart */
const MIN_BAR_ROW_HEIGHT = 36;

export {BAR_CORNER_RADIUS, MIN_BAR_ROW_HEIGHT};
export default BAR_INNER_PADDING;
