/** Inner padding between bars (0.3 = 30% of bar width) */
const BAR_INNER_PADDING = 0.3;

/** Extra top padding reserved for horizontal bar chart titles/legend clearance */
const HORIZONTAL_CHART_TOP_PADDING = 8;

/** Minimum vertical space per row in horizontal bar charts */
const HORIZONTAL_ROW_HEIGHT = 28;

export default BAR_INNER_PADDING;
export {HORIZONTAL_CHART_TOP_PADDING, HORIZONTAL_ROW_HEIGHT};
