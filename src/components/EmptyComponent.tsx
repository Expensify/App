/**
 * Renders nothing. Useful as a placeholder for navigator screens whose rendering is
 * handled elsewhere (e.g. sidebar screens that are rendered outside the navigator
 * on wide layouts via a sibling sidebar component).
 */
function EmptyComponent() {
    return null;
}

export default EmptyComponent;
