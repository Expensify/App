import type {ViewStyle} from 'react-native';

type GetReceiptImageRestrictedStyle = (maxWidth: number, availableWidth: number, aspectRatio?: number | null) => ViewStyle;

export default GetReceiptImageRestrictedStyle;
