type CenteredModalLayoutOverlayProps = {
    /** Called when the backdrop (area outside the card) is pressed, before navigating back */
    onBackdropPress: () => void;
};

export default CenteredModalLayoutOverlayProps;
