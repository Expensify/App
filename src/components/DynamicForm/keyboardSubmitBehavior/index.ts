import CONST from '@src/CONST';

/** Dynamic form pages navigate on submit, so the keyboard is dismissed alongside the navigation rather than before it */
export default CONST.KEYBOARD_SUBMIT_BEHAVIOR.SUBMIT_AND_DISMISS;
