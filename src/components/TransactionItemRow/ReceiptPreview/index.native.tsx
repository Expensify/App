// eslint-disable-next-line @typescript-eslint/no-unused-vars -- to keep the same function definition between mobile and web
function ReceiptPreview({source, hovered}: {source: string; hovered: boolean}) {
    return null;
}

export default ReceiptPreview;
