import PropTypes from 'prop-types';

const propTypes = {
    /** Whether or not to show the disabled styles */
    isDisabled: PropTypes.bool,
};
const defaultProps = {
    isDisabled: false,
};

export {
    propTypes,
    defaultProps,
};
