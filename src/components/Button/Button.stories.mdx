import {Meta, Story, Canvas, ArgsTable} from '@storybook/addon-docs/blocks';
import Button from './Button';

<Meta title="Components/Button" component={Button} />

# Button

Classic button with text and optional loading spinner.

## Default
<Canvas>
    <Story
        name="Default"
        args={{
            text: 'Submit',
            isLoading: false,
        }}
    >
    {args => <Button {...args} />}
    </Story>
</Canvas>

<ArgsTable story="Default" />

## Loading
<Canvas>
    <Story
        name="Loading"
        args={{
            text: 'Submit',
            isLoading: true,
            size: 'large',
        }}
    >
    {args => <Button {...args} />}
    </Story>
</Canvas>

## Small
<Canvas>
    <Story
        name="Small"
        args={{
            text: 'Submit',
            isLoading: false,
            size: 'small',
        }}
    >
    {args => <Button {...args} />}
    </Story>
</Canvas>

## Small (Loading)
<Canvas>
    <Story
        name="Small (Loading)"
        args={{
            text: 'Submit',
            isLoading: true,
            size: 'small',
        }}
    >
    {args => <Button {...args} />}
    </Story>
</Canvas>