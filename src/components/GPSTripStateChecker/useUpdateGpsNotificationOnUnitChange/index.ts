// Only iOS needs notification updates on unit change, as Android GPS notification doesn't
// have unit-dependent data
function useUpdateGpsNotificationOnUnitChange() {}

export default useUpdateGpsNotificationOnUnitChange;
