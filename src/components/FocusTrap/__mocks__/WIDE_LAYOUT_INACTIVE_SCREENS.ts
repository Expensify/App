/**
 * Mock for WIDE_LAYOUT_INACTIVE_SCREENS used in tests.
 * These are screens where focus trap should be inactive in wide layout mode.
 */
const WIDE_LAYOUT_INACTIVE_SCREENS: string[] = ['Home', 'Report'];

export default WIDE_LAYOUT_INACTIVE_SCREENS;
