/**
 * Mock for TOP_TAB_SCREENS used in tests.
 * These are screen names where focus trap activation depends on isFocused state.
 */
const TOP_TAB_SCREENS: string[] = ['NewChat', 'NewRoom'];

export default TOP_TAB_SCREENS;
