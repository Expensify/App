import type {Card} from '@src/types/onyx';

import type {StyleProp, ViewStyle} from 'react-native';

type AddToWalletStatusTextProps = {
    card: Card;
    style?: StyleProp<ViewStyle>;
};

export default AddToWalletStatusTextProps;
