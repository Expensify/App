import variables from '@styles/variables';

/**
 * Width the member cell's avatar and the gap after it take before the name and subtitle start. Shared by every table
 * whose member column measures its content, so the avatar is reserved the same room in all of them.
 */
const MEMBER_CELL_AVATAR_WIDTH = variables.avatarSizeSmall + 12;

export default MEMBER_CELL_AVATAR_WIDTH;
