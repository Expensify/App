/** Model of Expensify card bank account metadata for a workspace */
type ExpensifyCardBankAccountMetadata = {
    /** Whether we are loading the data via the API */
    isLoading?: boolean;

    /** Whether the request was successful */
    isSuccess?: boolean;
};

export default ExpensifyCardBankAccountMetadata;
