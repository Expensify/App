/** Information about vacation delegate */
type VacationDelegate = {
    /** Email of the user that set the vacation delegate */
    creator: string;

    /** Email of the vacation delegate */
    delegate: string;
};

export default VacationDelegate;
