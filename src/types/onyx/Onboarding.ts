
type Onboarding = {
    /** True if this user completed the onboarding flow, undefined if they are old user which did not go through the flow yet */
    hasCompletedGuidedSetupFlow?: boolean;
};

export default Onboarding;
