type PolicyCategory = {
    /** Name of a category */
    name: string;

    /** Flag that determines if a category is active and able to be selected */
    enabled: boolean;
};

export default PolicyCategory;
