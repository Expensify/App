type Attachment = {
    attachmentID: string;

    /** Local source of the attachment */
    source?: string;

    /** Remote source url of the attachment */
    remoteSource?: string;
};

export default Attachment;
