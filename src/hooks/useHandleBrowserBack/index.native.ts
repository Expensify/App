import type UseHandleBrowserBackCallback from './types';

// Browser back button handling is only needed on web
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function useHandleBrowserBack(_callback: UseHandleBrowserBackCallback, _isActive: boolean) {}
