/** Native: popstate doesn't exist, so the callback never fires. */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function usePopstateListener(isActive: boolean, onPopstate: () => void) {}
