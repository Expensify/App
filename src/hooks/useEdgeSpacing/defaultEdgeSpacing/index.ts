import type {EdgeInsets} from 'react-native-safe-area-context';

const defaultEdgeSpacing: EdgeInsets = {
    top: 20,
    left: 20,
    right: 20,
    bottom: 20,
};

export default defaultEdgeSpacing;
