/**
 * This is just a local copy of the EventType enum from 'urbanairship-react-native'.
 * Importing it on any index.js file breaks web, so we'll export this from PushNotifcation/index.js instead.
 * Update it as necessary.
 */
export default {
    NotificationResponse: 'notificationResponse',
    PushReceived: 'pushReceived',
};
