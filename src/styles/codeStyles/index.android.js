const codeWordWrapper = {
    height: 10,
};

const codeWordStyle = {
    top: -1,
    marginVertical: -2,
};

const codeTextStyle = {
    lineHeight: 18,
};

export default {codeWordWrapper, codeWordStyle, codeTextStyle};
