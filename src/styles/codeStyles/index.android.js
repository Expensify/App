const codeWordWrapper = {
    height: 20,
};

const codeWordStyle = {
    height: 18,
    top: 4,
};

const codeTextStyle = {
    lineHeight: 15,
};

export default {codeWordWrapper, codeWordStyle, codeTextStyle};
