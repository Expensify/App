const codeWordWrapper = {
    height: 10,
};

const codeWordStyle = {
    top: 1,
    marginVertical: -4,
};

const codeTextStyle = {
    lineHeight: 15,
};

export default {codeWordWrapper, codeWordStyle, codeTextStyle};
