import type WordBreakStyles from './types';

const wordBreak: WordBreakStyles = {
    breakWord: {},
    breakAllNonNative: {},
    breakAll: {
        wordBreak: 'break-all',
    },
};

export default wordBreak;
