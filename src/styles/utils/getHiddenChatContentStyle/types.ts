import type {ViewStyle} from 'react-native';
import type {ThemeStyles} from '@src/styles';

type GetHiddenChatContentStyle = (styles: ThemeStyles) => ViewStyle;

export default GetHiddenChatContentStyle;
