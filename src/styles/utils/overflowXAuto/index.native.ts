import type OverflowXAutoStyles from './types';

// Horizontal overflow scrolling doesn't exist in react-native, so this is a no-op on native.
const overflowXAuto: OverflowXAutoStyles = {};

export default overflowXAuto;
