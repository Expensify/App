import type UserSelectStyles from './types';

const userSelect: UserSelectStyles = {
    userSelectAuto: {
        userSelect: 'auto',
    },
    userSelectText: {
        userSelect: 'text',
    },
    userSelectNone: {
        userSelect: 'none',
    },
};

export default userSelect;
