import type {ViewStyle} from 'react-native';
import type {ThemeStyles} from '@styles/index';

type SplitAmountInputStyles = (styles: ThemeStyles, isSmallScreenWidth: boolean) => ViewStyle[];

export default SplitAmountInputStyles;
