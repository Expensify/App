// Since we don't use multiple font families on native platforms, we just fallback to single.
// eslint-disable-next-line no-restricted-imports
import fontFamily from '@styles/utils/FontUtils/fontFamily/singleFontFamily';

export default fontFamily;
