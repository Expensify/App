import type GetUserListItemStyles from './types';

const getUserListItemStyles: GetUserListItemStyles = (styles) => [styles.flex1, styles.flexColumn, styles.justifyContentCenter, styles.alignItemsStart, styles.optionRow];

export default getUserListItemStyles;
