import type {ViewStyle} from 'react-native';
import type {ThemeStyles} from '@styles/index';

type GetUserListItemStyles = (styles: ThemeStyles) => ViewStyle[];

export default GetUserListItemStyles;
