/**
 * Android platform supports the "hamburgerInputPadding" property, so this
 * can be added to the object
 */

import styles from '../styles';

function hamburgerInputPadding() {
    return styles.hamburgerOpenAbsolute;
}

export default hamburgerInputPadding;
