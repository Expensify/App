/**
 * Web, desktop and iOS platforms don't support the "hamburgerInputPadding" property, so it
 * function is a no-op
 */

function hamburgerInputPadding() {
    return null;
}

export default hamburgerInputPadding;
