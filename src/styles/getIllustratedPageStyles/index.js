import baseStyles from './baseStyles';
import themeColors from '../themes/default';

baseStyles.illustratedPageHeader = {
    flex: 9,
    minHeight: 270,
};

baseStyles.illustratedPageBody = {
    flex: 16,
    backgroundColor: themeColors.appBG,
};

export default baseStyles;
