import type {ReactNode} from 'react';

type StaticReportActionsPreviewProps = {
    children: ReactNode;

    /** Whether to show the bottom spacer (gradient overlay at the bottom) */
    showSpacer: boolean;
};

export default StaticReportActionsPreviewProps;
