import type {ButtonProps} from '@components/Button';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
function CardSectionButton(_props: ButtonProps) {
    return null;
}

export default CardSectionButton;
