import PropTypes from 'prop-types';

export default {
    // HTML of the COMMENT reportAction
    html: PropTypes.string.isRequired,

    // Text of the COMMENT reportAction
    text: PropTypes.string.isRequired,
};
