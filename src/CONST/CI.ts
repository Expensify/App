// DO NOT EDIT - Values are injected by .github/scripts/inject-ci-data.sh during CI builds.
// Default empty values are committed; CI overwrites them before bundling JS.
const PULL_REQUEST_NUMBER = '';

const CI = {
    PULL_REQUEST_NUMBER,
};

export default CI;
