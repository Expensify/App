{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "types": ["@types/bun"],
        "paths": {
            "@scripts/*": ["../../scripts/*", "../../.github/scripts/*"]
        }
    },
    "include": ["src", "tests", "stubs", "scripts"]
}
