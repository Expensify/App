name: Create or update deploy checklist

on:
  workflow_call:
    inputs:
      REF:
        description: 'Git ref (branch, tag, or SHA) to check out. Defaults to the caller commit SHA.'
        type: string
        required: false
  workflow_dispatch:

jobs:
  createChecklist:
    runs-on: blacksmith-2vcpu-ubuntu-2404
    steps:
      - name: Checkout
        uses: useblacksmith/checkout@1c9394c220d293645707b625ba9d79685f093a8f # v1
        with:
          ref: ${{ inputs.REF || github.sha }}

      - name: Setup Node
        uses: ./.github/actions/composite/setupNodeStickyDisk

      - name: Create or update deploy checklist
        run: npx bun scripts/createOrUpdateDeployChecklist.ts
        env:
          GITHUB_TOKEN: ${{ secrets.OS_BOTIFY_TOKEN }}
