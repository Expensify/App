name: Build and Deploy Android

# This workflow is run when any tag is published
on:
    push:
        tags:
            - '*'

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-ruby@v1

        - name: Install bundler
          run: gem install bundler

        - name: Install gems
          run: bundle install

        - name: Install node packages
          run: npm install

        # TODO: Delete this step once Android app is approved, see: https://github.com/Expensify/ReactNativeChat/issues/290
        - name: Configure AWS Credentials
          uses: aws-actions/configure-aws-credentials@v1
          with:
              aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
              aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
              aws-region: us-east-1

        - name: Decrypt keystore
          run: cd android/app && gpg --quiet --batch --yes --decrypt --passphrase="$LARGE_SECRET_PASSPHRASE" --output my-upload-key.keystore my-upload-key.keystore.gpg
          env:
              LARGE_SECRET_PASSPHRASE: ${{ secrets.LARGE_SECRET_PASSPHRASE }}

        - name: Decrypt json key
          run: cd android/app && gpg --quiet --batch --yes --decrypt --passphrase="$LARGE_SECRET_PASSPHRASE" --output android-fastlane-json-key.json android-fastlane-json-key.json.gpg
          env:
              LARGE_SECRET_PASSPHRASE: ${{ secrets.LARGE_SECRET_PASSPHRASE }}

        # TODO: Delete this step once Android app is approved, see: https://github.com/Expensify/ReactNativeChat/issues/290
        - name: Run Fastlane build
          run: bundle exec fastlane android build

        # TODO: Delete this step once Android app is approved, see: https://github.com/Expensify/ReactNativeChat/issues/290
        - name: Deploy to S3
          run: aws s3 cp --acl public-read $GITHUB_WORKSPACE/android/app/build/outputs/apk/release/app-release.apk s3://chat-test-expensify-com/app-release.apk

        # TODO: Uncomment once Android app is approved, see: https://github.com/Expensify/ReactNativeChat/issues/290
        # - name: Run Fastlane beta
        #   run: bundle exec fastlane android beta
