name: 'Compress svg'
description: 'Compress svg files with svgo'
inputs:
  GITHUB_TOKEN:
    description: Auth token for New Expensify Github
    required: true
runs:
  using: 'node20'
  main: './index.js'
