name: Set up Node
description: Set up Node

runs:
  using: composite
  steps:
    - uses: actions/setup-node@8c91899e586c5b171469028077307d293428b516
      with:
        node-version-file: '.nvmrc'
        cache: npm
        cache-dependency-path: |
          package-lock.json
          desktop/package-lock.json

    - id: cache-node-modules
      uses: actions/cache@v3
      with:
        path: node_modules
        key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}

    - id: cache-desktop-node-modules
      uses: actions/cache@v3
      with:
        path: desktop/node_modules
        key: ${{ runner.os }}-desktop-node-modules-${{ hashFiles('desktop/package-lock.json') }}

    - name: Check if patch files changed
      id: patchCheck
      shell: bash
      run: |
        set -e
        if [[ `git diff main --name-only | grep \.patch` != null ]]; then 
          echo 'CHANGES_IN_PATCH_FILES=true' >> "$GITHUB_OUTPUT"
        else 
          echo 'CHANGES_IN_PATCH_FILES=false' >> "$GITHUB_OUTPUT"
        fi

    - name: Patch root project node packages
      shell: bash
      id: patch
      if: | 
        steps.patchCheck.outputs.CHANGES_IN_PATCH_FILES == 'true' && 
        steps.cache-node-modules.outputs.cache-hit == 'true'
      run: |
        {
          set +e && npx patch-package &&
          echo 'PATCH_EXECUTED=true' >> "$GITHUB_OUTPUT"
        } || {
          echo 'PATCH_EXECUTED=false' >> "$GITHUB_OUTPUT"
        }

    - name: Install root project node packages if patching failed
      if: steps.patch.outputs.PATCH_EXECUTED == 'false'
      uses: nick-fields/retry@v2
      with:
        timeout_minutes: 30
        max_attempts: 3
        command: npm ci

    - name: Patch node packages for desktop submodule
      shell: bash
      id: patchDesktop
      if: | 
        steps.patchCheck.outputs.CHANGES_IN_PATCH_FILES == 'true' && 
        steps.cache-desktop-node-modules.outputs.cache-hit == 'true'
      run: |
        set +e
        {
          cd desktop && npx patch-package &&
          echo 'PATCH_EXECUTED=true' >> "$GITHUB_OUTPUT"
        } || {
          echo 'PATCH_EXECUTED=false' >> "$GITHUB_OUTPUT"
        }

    - name: Install node packages for desktop submodule if patching failed
      if: steps.patchDesktop.outputs.PATCH_EXECUTED == 'false'
      uses: nick-fields/retry@v2
      with:
        timeout_minutes: 30
        max_attempts: 3
        command: cd desktop && npm ci

    - name: Install root project node packages
      if: steps.cache-node-modules.outputs.cache-hit != 'true'
      uses: nick-fields/retry@v2
      with:
        timeout_minutes: 30
        max_attempts: 3
        command: npm ci

    - name: Install node packages for desktop submodule
      if: steps.cache-desktop-node-modules.outputs.cache-hit != 'true'
      uses: nick-fields/retry@v2
      with:
        timeout_minutes: 30
        max_attempts: 3
        command: cd desktop && npm ci
